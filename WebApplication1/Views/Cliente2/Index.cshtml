@model IEnumerable<WebApplication1.Models.Cliente2>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Idade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Idade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Clente2Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Clente2Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Clente2Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
